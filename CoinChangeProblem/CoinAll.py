class CoinAll:
    def __init__(self, coins):
        """
        :param coins: List of coin denominations (e.g. [1, 2])
        """
        self.coins = coins

    def all_combinations(self, target):
        """
        Return all possible combinations of coins that sum to target.
        Each combination is a list of coins (e.g. [1, 1, 2])
        :param target: The amount to make
        :return: List of combinations (list of lists)
        """
        # TODO: implement backtracking to find all valid combinations
        return []
